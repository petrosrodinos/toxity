import { SectionPlaceholder } from "@/pages/app/components/section-placeholder";

export default function HistoryPage() {
    return (
        <SectionPlaceholder
            title="History"
            description="Your recent scans will appear here once product lookup is live."
        />
    );
}
