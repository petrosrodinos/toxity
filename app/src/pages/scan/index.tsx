import { SectionPlaceholder } from "@/pages/app/components/section-placeholder";

export default function ScanPage() {
    return (
        <SectionPlaceholder
            title="Scan"
            description="Point your camera at a barcode to look up ingredient safety."
        />
    );
}
