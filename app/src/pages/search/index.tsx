import { SectionPlaceholder } from "@/pages/app/components/section-placeholder";

export default function SearchPage() {
    return (
        <SectionPlaceholder
            title="Search"
            description="Find products, ingredients, and brands across the Toxity library."
        />
    );
}
